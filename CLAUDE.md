# OpenEscapement

@AGENTS.md

Canonical agent instructions live in `AGENTS.md` (imported above) so every agent reads one source — the same convention this product enforces for its users. Add guidance here only if it is Claude-specific and cannot live in `AGENTS.md`.
